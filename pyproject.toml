[project]
name = "equator"
version = "0.1.0"
description = "Render LaTeX Math to PNG"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cairosvg>=2.9.0",
    "fastapi[standard]>=0.135.3",
    "matplotlib>=3.10.8",
    "ziamath>=0.13",
]
