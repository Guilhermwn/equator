[project]
name = "equator"
version = "0.1.0"
description = "Render LaTeX Math to PNG"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.135.3",
    "htpy>=25.12.0",
]
